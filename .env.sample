# VM Control Server Configuration
# This file should be placed in the same directory as vm_control_server.py
# on the Hyper-V host (Computer B)

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# API Key
# Generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=your-super-secret-api-key-change-this

# HMAC Secret - Must match VM_HMAC_SECRET in the Telegram bot's configuration
# Generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
HMAC_SECRET=your-hmac-secret-change-this

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# IP Whitelisting - Only allow requests from this IP address
# Leave empty or comment out to allow requests from any IP
# Example: ALLOW_IP=192.168.x.x
ALLOW_IP=

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. Generate strong random keys for API_KEY and HMAC_SECRET
# 2. Keep these values SECRET - never commit to version control
# 3. Use the SAME values in both server and bot configurations
# 4. Rotate keys periodically for security
# 5. Use HTTPS in production (configure in uvicorn command)
# 6. Consider enabling ALLOW_IP for additional security
# 7. Monitor logs/audit.log regularly for suspicious activity
# 8. Run the server with appropriate Windows permissions
# 9. Set up firewall rules to restrict access to port 8000
# 10. Consider running as a Windows service for production

# =============================================================================
# GENERATING SECURE KEYS
# =============================================================================
# Option 1 - Python:
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# Option 2 - OpenSSL:
#   openssl rand -hex 32
#
# Option 3 - PowerShell:
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | % {[char]$_})

# =============================================================================
# EXAMPLE (DO NOT USE THESE VALUES IN PRODUCTION)
# =============================================================================
# API_KEY=randomrandomrandomrandomrandomrandomrand
# HMAC_SECRET=randomrandomrandomrandomrandomrandomrand
# ALLOW_IP=192.168.x.x
